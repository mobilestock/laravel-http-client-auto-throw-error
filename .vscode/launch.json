{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/var/www/html/app": "${workspaceFolder}/apps/adm-api",
        "/lookpay-api": "${workspaceFolder}/apps/lookpay-api",
        "/var/lib/shared/": "${workspaceFolder}/shared",
        "/med-api": "${workspaceFolder}/apps/med-api"
      }
    }
  ]
}
